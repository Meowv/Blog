@page "/posts"
@page "/posts/{PageIndex}"

<h3>Posts</h3>

当前页：@PageIndex

@code {
    [Parameter]
    public string PageIndex { get; set; }

    protected override void OnInitialized()
    {
        if (PageIndex == "")
        {
            PageIndex = "1";
        }
    }
}